#include "Pods-UIView+Copy.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/UIView+Copy" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/UIView+Copy"
OTHER_LDFLAGS = -ObjC
PODS_ROOT = ${SRCROOT}